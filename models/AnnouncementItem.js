class Announcement {
  constructor(id, moduleId, title, body) {
    this.id = id;
    this.moduleId = moduleId;
    this.title = title;
    this.body = body;
  }
}

export default Announcement;
